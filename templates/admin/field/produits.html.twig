{% block _Product_roles %}

{% for produit in field.value %}
   {{ produit.id }} {{ produit.titre }}
   <img src="{{ asset('images/produits/' ~ produit.photo) }}" alt= "{{ produit.titre }}" class="img-fluid" " width="50px" height="50px">
{% endfor %}

{% endblock %}